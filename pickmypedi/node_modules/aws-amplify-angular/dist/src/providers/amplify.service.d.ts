import { Subject, Observable } from 'rxjs';
import { Logger, AuthClass, AnalyticsClass, StorageClass, APIClass, InteractionsClass } from 'aws-amplify';
import { AuthState } from './auth.state';
export declare class AmplifyService {
    private _auth;
    private _analytics;
    private _storage;
    private _api;
    private _cache;
    private _pubsub;
    private _interactions;
    private _logger;
    private _xr;
    private _authState;
    authStateChange$: Observable<AuthState>;
    constructor();
    auth(): AuthClass;
    analytics(): AnalyticsClass;
    storage(): StorageClass;
    api(): APIClass;
    interactions(): InteractionsClass;
    cache(): any;
    pubsub(): any;
    logger(name: any, level: any): Logger;
    xr(): any;
    authState(): Subject<AuthState>;
    setAuthState(state: AuthState): void;
}
