import { AmplifyService, AuthState } from '../../../providers';
export declare class GreetingComponentCore {
    signedIn: boolean;
    greeting: string;
    amplifyService: AmplifyService;
    constructor(amplifyService: AmplifyService);
    authState: AuthState;
    subscribe(): void;
    setAuthState(authState: AuthState): void;
    onSignOut(): void;
}
